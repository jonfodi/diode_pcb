load("@stdlib/properties.zen", "Layout")

MyModule = Module("Module.zen")

P1 = Net("BOARD_P1")
P2 = Net("BOARD_P2")
P3 = Net("BOARD_P3")
P4 = Net("BOARD_P4")

MyModule(name = "M1", P1 = P1, P2 = P2)

MyModule(name = "M2", P1 = P3, P2 = P4)

Layout(name = "Board", path = "build/layout")
